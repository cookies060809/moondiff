// 根包是库包（默认 pkgtype kind: "lib"），无需显式声明 pkgtype。
//
// `Json` 类型和 `stringify` 在 builtin 里，造 JSON 不用 import。
// 只有解析接口响应要用 `moonbitlang/core/json` 的 parse，所以只 import 它。

import {
  "moonbitlang/core/json",
}
